# 작업 규칙

## 작업 시작

- `README.md`에서 문서 역할과 저장 경로를 확인합니다.
- `references/course/00_README.md`와 작업에 해당하는 작성지침을 읽습니다. 예시의 형식은 활용하되 도메인·수치·조건은 우리 근거에서 가져옵니다.
- 관련 산출물이 있으면 먼저 읽습니다. 인터뷰·관찰은 `docs/research/interviews.md`, 도메인 정의는 `docs/ontology.yaml`, 제품 범위와 수용 기준은 `docs/SPEC.md`를 기준으로 합니다.
- 자료가 확보되거나 산출물을 실제 작성할 때 필요한 경로를 만듭니다. 빈 파일·폴더를 미리 만들지 않습니다.

## 용어와 정의

- **인터뷰 로그**: 참여자가 설명한 경험과 발언을 기록한 자료. 로그 번호와 원문 위치로 식별합니다.
- **관찰 기록**: 직접 확인한 행동·화면·작업물의 기록. 관찰 번호를 붙이고 해석과 구분합니다.
- **가설**: 근거를 통해 확인 중인 설명. 사실이나 확정 요구사항으로 취급하지 않습니다.
- **Job Story**: 사용자의 상황·동기·기대 결과를 근거와 함께 정리한 문장입니다.
- **수용 기준(AC)**: 구현의 완료 여부를 판정할 구체적인 조건입니다.
- 도메인 클래스·속성·관계의 대표어는 온톨로지에서 정하고 스키마·스펙·코드·테스트에 동일하게 사용합니다. 동의어나 비슷한 이름을 임의로 추가하지 않습니다.

## 근거와 결정

- 실제 발언, 참여자의 추측, 직접 관찰, 팀의 해석을 구분합니다. 원문에 없는 경험·감정·시간·비용을 만들지 않습니다.
- 로그·관찰·AC 번호는 유지합니다. 태그와 분석에서 원문으로, 요구사항과 온톨로지에서 해당 근거로 돌아갈 수 있어야 합니다.
- 모의 인터뷰와 실제 인터뷰를 구분합니다. 같은 사람의 여러 사건을 여러 인터뷰로 집계하지 않습니다.
- 온톨로지의 클래스·속성·관계에 `evidence`를 연결하고 관계 대상이 정의된 클래스인지 확인합니다.
- 인터뷰와 관찰은 사람이 수행합니다. AI는 원문 정리와 분석·개념 후보 작성을 보조할 수 있습니다.
- 문제 정의, 도메인 경계, 스키마·파싱 규칙, 수용 기준, 절대 규칙, 골든 케이스, 평가 지표는 팀이 결정합니다. 미결정 내용을 AI가 임의로 확정하지 않습니다.
- 초안·미확인·팀 검토 완료를 구분합니다. 참고자료의 예시와 미검증 후보를 제품 기준에 섞지 않습니다.

## 파일과 변경

- 참고 원본은 보존하고 우리 산출물을 지정 경로에 작성합니다.
- 제품 프롬프트는 `src/<패키지>/prompts/`, 개발 위임 기록은 `docs/prompts/`, 평가 프롬프트는 `evals/`에 둡니다.
- 파싱 프롬프트의 전송부는 `<!-- prompt:start -->`와 `<!-- prompt:end -->`로 구분합니다. 전송부 변경 시 `prompt_version`과 변경 이력을 갱신합니다.
- 온톨로지 변경 시 스키마·스펙·코드·테스트의 용어와 구조를 함께 확인합니다. AC 변경 시 관련 골든 케이스와 평가 기준도 확인합니다.
- 버전은 Git으로 관리합니다. 기존 사용자 변경을 보존하고, 지침·설정 변경 전 백업은 저장소 밖에 둡니다.
- 인터뷰 자료는 비식별화합니다. 실명·연락처·녹음 등 비식별화되지 않은 원자료를 저장소에 넣지 않습니다.

## 완료 기준

- 요청한 내용이 지정 파일에 반영되어 있어야 합니다.
- 인용·근거 번호·파일 링크가 유효하고 관련 산출물의 용어와 내용이 일치해야 합니다.
- 문서는 내용·근거·참조를 점검합니다. 코드와 실행 설정은 변경 범위에 해당하는 테스트·검증을 수행합니다.
- 구현은 팀이 정한 수용 기준으로 판정합니다. 실행·테스트 명령은 실제 확인한 뒤 README에 기록합니다.
- 수행한 검증과 결과, 미확인·미실행 항목을 구분해 보고합니다. 초안이나 로컬 검증을 최종 제출·배포 완료로 표현하지 않습니다.
