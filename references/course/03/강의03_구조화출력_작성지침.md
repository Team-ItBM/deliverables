# [강의 3] 구조화 출력 — 산출물 작성 지침

> 공통 예제 **단골**이 "자연어 발화 → 구조화된 JSON"을 실제로 어떻게 설계했는지 보고 같은 자리에 우리 팀 것을 채운다.
> 이 산출물은 강의 2의 온톨로지를 **실행 가능한 계약**으로 바꾸는 단계다. 온톨로지에 없던 개념이 여기서 튀어나오면 둘 중 하나가 틀린 것이다.

## 이번 강의에서 만드는 것

| # | 산출물 | 형식 | 무엇을 담는가 | 저장소 위치 | 누가 쓰는가 | 선행 산출물 | 이후 어디에 쓰이나 |
|---|---|---|---|---|---|---|---|
| 1 | 출력 스키마 | JSON Schema | 파싱 결과의 구조 계약 — 필드, 타입, enum, required, null 허용 | `src/<패키지>/schemas/<클래스>.schema.json` | 👤 사람 | 강의 2 온톨로지의 요청 컨텍스트 클래스 | 강의 5 골든 케이스의 `context` / 강의 6 도구 시그니처 / 강의 7 형식 준수율 |
| 2 | 파싱 프롬프트 | Markdown | ① 런타임에 모델로 전송되는 시스템 지시와 입출력 예시 2개 ② 필드 → 함수 인자 표 ③ 실패 모드 점검표 ④ 버전과 변경 이력 | `src/<패키지>/prompts/parse_query.md` | 👤 사람 | 1의 스키마 | 강의 6 파이프라인의 첫 단계 / 강의 7 오류 분석의 출발점 |

> 파일 이름은 팀 규칙에 맞게 바꿔도 되지만 **저장 위치는 맞춘다** — 채점과 리뷰 때 이 경로를 본다. 저장소 전체 구조는 `00_README.md`의 "저장소 표준 구조"를 본다.
>
> **누가 쓰는가 — 👤 사람.** 두 파일 모두 파서의 판정 기준이다. 기준을 에이전트가 정하면 그 기준으로 에이전트의 결과를 검증할 수 없다. 파서 구현(강의 5)은 에이전트에게 맡기되, 이 두 파일은 사람이 쓰고 사람이 고친다.

## 파싱 프롬프트 파일은 어떻게 쓰이나

이 부분이 자주 헷갈리므로 먼저 못 박는다.

- **`parse_query.md`는 코드가 읽는 런타임 파일이다.** 파서 코드는 이 파일을 열어 `<!-- prompt:start -->`와 `<!-- prompt:end -->` 사이의 텍스트만 system 프롬프트로 보낸다. 마커 밖(필드 → 함수 표, 실패 모드 점검표, 변경 이력)은 팀이 읽는 문서이며 모델에게 가지 않는다. 한 파일에 두는 이유는 "무엇을 보내는가"와 "왜 그렇게 보내는가"가 갈라지지 않게 하기 위해서다.
- **프롬프트를 코드 안 문자열로 두지 않는다.** 코드를 열지 않아도 프롬프트를 읽을 수 있어야 하고, 강의 7의 측정 기록이 "어느 버전의 프롬프트였나"를 가리킬 수 있어야 한다.
- **버전은 git이 관리한다.** 옛 버전 파일을 `parse_query_v1.md`처럼 따로 남기지 않는다. 대신 파일 머리의 `prompt_version`을 마커 안이 바뀔 때마다 올리고 변경 이력에 한 줄 적는다. 측정 기록에는 모델 ID, `prompt_version`, 스키마 커밋을 함께 적는다.
- **`src/<패키지>/prompts/`에는 제품이 런타임에 읽는 프롬프트만 둔다.** 기능당 한 파일이다. 강의 3 시점에는 `parse_query.md` 하나이고, 이후 기능이 붙으면 파일이 늘어난다. 사람이 코딩 에이전트에게 주는 위임 프롬프트는 `docs/prompts/`(강의 5), evals의 judge 프롬프트는 `evals/`(강의 7)에 둔다 — 제품이 부르는 프롬프트와 개발 과정의 프롬프트를 섞지 않는다.
- **모델이 알 수 없는 기준 정보는 코드가 채운다.** 오늘 날짜처럼 프롬프트에 필요하지만 모델이 모르는 값은 `{today}` 같은 자리를 두고 호출 직전에 코드가 채운다. 프롬프트 파일에 날짜를 직접 적지 않는다.

## 이 파일 읽는 법

| 표시 | 뜻 |
|---|---|
| 📘 **단골 예시** | 제출 가능한 수준으로 채워진 예. |
| 💬 **왜 이렇게 썼나** | 판단 근거. 채점 기준과 직결된다. |
| 🔁 **반드시 달라지는 곳** | 도메인이 다르면 달라지는 자리. 그대로 옮기면 감점. |
| 🧩 **따라 해도 되는 곳** | 형식과 규약. 맞추는 편이 좋다. |
| ✍️ **우리 팀** | 여러분이 채우는 칸. |

**함께 보는 자료** — 단골 산출물 두 파일의 전문이다. 아래 📘 블록은 이 두 파일에서 발췌했다.
- `강의03_request_context.schema.json` — 산출물 1의 전문
- `강의03_parse_query.md` — 산출물 2의 전문

**복제에 대하여** — 형식(JSON Schema 골격, enum과 required와 null 허용의 사용법, 마커 구조, 필드 → 함수 표, 실패 모드 점검표)은 그대로 가져다 써도 된다. **필드 목록과 enum 값은 우리 온톨로지에서 나와야 한다.** `area`, `dietary`를 그대로 옮겨 적은 스키마는 우리 제품의 스키마가 아니다.

---

## A. 무엇을 구조화하나

### 📘 단골 예시

- **입력(사용자 발화 예시)**: "조용한 강남 비건 식당, 둘이 저녁 7시"
- **출력으로 뽑을 구조**: 강의 2 온톨로지의 `RequestContext` 클래스 — `area`, `party_size`, `vibe`, `dietary`, `occasion`, `budget`, `time`
- **각 필드가 어디로 가는가** — 이 표는 `parse_query.md`의 마커 밖 "필드 → 함수 인자" 절에 그대로 들어간다. **하드**는 위반 시 후보 탈락, **소프트**는 점수 가감이다.

| 필드 | 어느 함수의 어느 인자로 가는가 | 하드/소프트 |
|---|---|---|
| `area` | `rank(ctx)`의 지역 일치 가산점(`AREA_WEIGHT`) | 소프트. 단 수집 필수 — null이면 검색 전에 되묻는다 |
| `dietary` | `rank(ctx)` 내부 `satisfies_dietary()` — 불만족 식당은 후보에서 제외 | **하드** |
| `vibe` | `rank(ctx)`의 `mood_tags` 겹침 점수(`MOOD_WEIGHT`) | 소프트 |
| `budget` | `rank(ctx)`의 `price_range` 근접 점수(`PRICE_WEIGHT`) | 소프트 |
| `party_size` | `make_reservation(party_size=…)` — 예약 가능 여부 판단 | 랭킹 미사용, 예약 인자. 수집 필수 |
| `time` | `make_reservation(time_slot=…)` 후보 슬롯 산출의 입력 | 랭킹 미사용, 예약 인자 |
| `occasion` | (v1 미사용 — 점수에 반영하지 않음. 로그 8의 근거가 뚜렷해 v2용으로 유지) | — |

### 💬 왜 이렇게 썼나

- **"이 값을 어느 함수의 어느 인자로 넘기나"에 답하지 못하는 필드는 지운다.** 구조화 출력 설계에서 가장 흔한 실패는 "있으면 좋을 것 같은" 필드를 20개 넣는 것이다. 모델은 채우라면 채우고, 채우려고 지어낸다. 표의 가운데 열이 비면 그 필드는 환각 생성기다.
- **하드와 소프트를 표에서 정했다.** `dietary`만 하드 필터다 — 알레르기와 종교 제약은 "덜 맞는" 식당이 아니라 "가면 안 되는" 식당을 가른다. `area`는 가산점(소프트)이지만 없으면 검색 자체를 시작하지 않는 수집 필수 조건이라, 두 축(랭킹에서의 취급 / 수집 필수 여부)이 다르다는 것을 표에 함께 적었다. 강의 5의 골든 테스트가 이 구분을 그대로 검사한다.
- **`occasion`은 미사용인데도 남겼고, 남긴 이유를 적었다.** 로그 8의 근거가 뚜렷해 v2에서 쓸 것이 확실하기 때문이다. 이유 없이 남긴 필드와 이유를 적고 남긴 필드는 다르다.
- **입력 예시가 실제 발화체다.** "강남 지역 비건 식당 2인 19시"처럼 이미 정돈된 문장을 예시로 쓰면 파서 설계가 쉬워 보이는 착시가 생긴다.

🔁 **반드시 달라지는 곳** — 발화 예시, 필드 목록, 함수 매핑 표 전부.

🧩 **따라 해도 되는 곳**
- "필드 → 함수 인자" 표를 만들고 `parse_query.md` 마커 밖에 두는 방식
- 하드/소프트 열을 두고 수집 필수 여부를 함께 적는 방식
- 미사용 필드에 이유를 병기하는 방식

### ✍️ 우리 팀

- 입력(사용자 발화 예시 — 실제 말투로): "____"
- 출력으로 뽑을 구조(강의 2 온톨로지의 어느 클래스인가): ____

| 필드 | 어느 함수의 어느 인자로 가는가 | 하드/소프트 |
|---|---|---|
| ____ | ____ | ____ |

---

## B. 출력 스키마 (JSON Schema)

### 📘 단골 예시 — `request_context.schema.json`

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RequestContext",
  "description": "단골: 사용자 자연어 발화를 파싱한 구조화 결과 (강의 3 산출물)",
  "type": "object",
  "properties": {
    "area": {"type": ["string", "null"], "description": "지역명. 모르면 null"},
    "party_size": {"type": ["integer", "null"], "minimum": 1},
    "vibe": {"type": "array", "items": {"type": "string"}, "description": "분위기 태그"},
    "dietary": {
      "type": "array",
      "items": {"type": "string", "enum": ["vegan", "vegetarian", "halal", "gluten_free"]},
      "description": "식이제약. 허용값만 — 환각 방지"
    },
    "occasion": {"type": "string"},
    "budget": {"type": ["integer", "null"], "description": "1인 예산(원). 모르면 null"},
    "time": {"type": "string", "description": "HH:MM, 24h"}
  },
  "required": ["area", "party_size"],
  "additionalProperties": false
}
```

### 💬 왜 이렇게 썼나

- **스키마의 프로퍼티가 온톨로지 `RequestContext`의 속성과 완전히 일치한다.** 이름 하나, 개수 하나까지 맞춘다. 어긋나면 코드에서 두 어휘가 자라나 결국 버그가 된다. 온톨로지를 고치면 이 파일도 같이 고친다(반대도 마찬가지).
- **`dietary`에만 `enum`을 걸었다.** 값의 집합이 닫혀 있고 그 값으로 하드 필터를 돌리기 때문이다. `vibe`는 열린 태그라 enum을 걸지 않았다 — 닫히지 않은 개념에 enum을 걸면 모델이 억지로 가장 가까운 값에 욱여넣는다. **enum은 "환각 방지 장치"이자 동시에 "표현 제약"이다.**
- **`required`가 `area`, `party_size` 두 개뿐이다.** `required`는 "값이 있어야 한다"가 아니라 "**키**가 있어야 한다"는 뜻이다. 모든 필드를 required로 걸면 모델은 모르는 값을 지어내서라도 키를 채운다. 그래서 **없으면 대화를 이어갈 수 없는 최소 집합**만 required로 두었다. 키는 항상 오되 값은 모를 수 있으므로 `area`, `party_size`의 타입을 `["string","null"]`, `["integer","null"]`로 열었다 — 그래야 정보 수집 중인 `{"area":null,"party_size":null}`이 검증을 통과하고, 애플리케이션이 그 상태를 보고 되묻는다.
- **null과 생략을 나눠 쓴다.** required 키는 모르면 null(키는 있고 값이 없음), 나머지는 키 자체를 생략한다. 이 정책은 스키마와 프롬프트 규칙이 같은 말을 해야 한다.
- **`additionalProperties: false`.** 스키마에 없는 키를 모델이 창작하는 것을 막는다. 이 한 줄이 빠지면 검증이 통과하면서도 예상 못 한 키가 흘러들어온다.
- **`minimum: 1`처럼 타입만이 아니라 값의 범위도 걸었다.** `party_size: 0`은 타입상 정수지만 의미상 불가능하다.
- **description을 사람이 아니라 모델에게 쓰는 지시로 썼다.** "모르면 null"은 모델이 읽는 문장이다. 다만 description은 검증 규칙이 아니다 — null을 실제로 허용하는 것은 `type`에 적힌 `"null"`이고, description만 적고 타입을 `"string"`으로 두면 null 값은 검증에서 떨어진다.

🔁 **반드시 달라지는 곳** — properties 전부, enum 값, required 집합.

🧩 **따라 해도 되는 곳**
- `additionalProperties: false`
- required를 "없으면 대화를 이어갈 수 없는 최소 집합"으로 두는 원칙
- 닫힌 집합에만 enum
- 모를 수 있는 필드는 `type`에 `"null"`을 함께 두고 description에 "모르면 null"류 지시를 넣는 방식
- 값 범위 제약(minimum/maximum)

### ✍️ 우리 팀

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "____",
  "description": "____",
  "type": "object",
  "properties": {
    "____": {"type": ["____", "null"], "description": "____"},
    "____": {"type": "array", "items": {"type": "string", "enum": ["____"]}}
  },
  "required": ["____"],
  "additionalProperties": false
}
```

- required를 이 집합으로 정한 이유: ____
- enum을 건 필드와 걸지 않은 필드, 그 이유: ____
- null과 생략을 어떻게 나누기로 했는가(어느 키가 null을 허용하고 어느 키를 생략하는가): ____

---

## C. 파싱 프롬프트 — 전송부

### 📘 단골 예시 — `parse_query.md`의 마커 안

```
너는 단골의 요청 파서다. 사용자 발화에서 식당 추천에 필요한 조건을 추출해 주어진 JSON 스키마에 정확히 맞는 객체 하나를 출력한다.

규칙:
- 발화에 명시되지 않은 값은 추측하지 않는다. area와 party_size는 모르면 null로 두고, 나머지 필드는 키 자체를 생략한다.
- area는 발화에 나온 지역명만 쓴다. 지역명을 지어내거나 넓혀 쓰지 않는다.
- party_size는 1 이상의 정수다. "둘이", "두 명"은 2다.
- dietary는 [vegan, vegetarian, halal, gluten_free] 중에서만 쓴다. "비건"은 vegan, "채식"은 vegetarian으로 매핑하고, 목록에 없는 식이제약은 dietary에 넣지 않는다.
- vibe는 발화에 나온 분위기 표현을 그대로 담는다.
- time은 24시간 HH:MM 형식이다. "저녁 7시"는 19:00이다. 오전인지 오후인지 알 수 없는 시각은 time을 생략한다.
- 출력은 JSON 객체 하나만 쓴다. 설명, 코드 펜스, 주석을 붙이지 않는다.

예시 1
입력: 조용한 강남 비건 식당, 둘이 저녁 7시
출력: {"area":"강남","party_size":2,"vibe":["조용한"],"dietary":["vegan"],"time":"19:00"}

예시 2
입력: 괜찮은 데 좀 알려줘
출력: {"area":null,"party_size":null}
```

### 💬 왜 이렇게 썼나

- **"추측하지 않는다"가 첫 번째 규칙이다.** LLM의 기본 성향은 빈칸을 메우는 것이다. 명시적으로 금지하지 않으면 "강남"이 없는 발화에서도 강남이 나온다.
- **null과 생략을 프롬프트에서도 나눴다.** 스키마의 required 집합과 같은 말을 한다. 스키마가 `area`를 null 허용 required로 두는데 프롬프트가 "모르면 생략"이라고 하면 키 누락으로 검증에 떨어진다.
- **enum 목록을 프롬프트에도 다시 적었다.** 스키마로 강제하더라도 프롬프트에 한 번 더 쓰면 위반 자체가 줄어든다. 스키마는 사후 검증이고 프롬프트는 사전 유도다 — 둘 다 한다.
- **매핑 규칙과 제외 규칙을 함께 지시했다.** "비건"은 `vegan`으로, "채식"은 `vegetarian`으로, "저탄고지"는 넣지 않는다. 이 지시가 없으면 모델이 `"저탄고지"`를 그대로 넣어 enum을 깨뜨린다. 제외된 조건이 사용자에게 어떻게 보이는지는 D절 5번에서 다룬다.
- **모호한 시각은 확정하지 않는다.** "저녁 7시"는 19:00이지만 "7시"만으로는 오전인지 오후인지 알 수 없다. 이때 19:00으로 채우면 스키마는 통과해도 조건 추출은 실패다. `time`을 생략하게 하고 예약 전에 되묻는다.
- **예시 2를 반드시 넣는다.** 정보가 충분한 예시만 주면 모델은 "빈칸을 남기는 출력"을 한 번도 본 적이 없게 된다. **"모를 때 어떻게 출력하는가"를 예시로 가르치는 것**이 이 산출물의 핵심이다.
- **"출력은 JSON 객체 하나만."** 설명이 섞이면 파싱이 깨진다. 모델이 친절해지려는 성향을 명시적으로 차단한다.
- **모델이 알 수 없는 기준 정보는 주입한다.** 상대 날짜("내일")를 다루려면 오늘 날짜가 필요하다. 이런 값은 `{today}` 자리를 두고 코드가 채우는 것이지 모델이 아는 것이 아니다. 단골 v1에는 날짜 필드가 없어 주입할 값이 없고, 그 사실을 파일에 적었다.

🔁 **반드시 달라지는 곳** — 시스템 지시 내용, 예시 발화와 출력 전부.

🧩 **따라 해도 되는 곳**
- "추측 금지 / 허용값 제한 / 지어내기 금지 / JSON만" 4종 지시
- null과 생략의 정책을 규칙 한 줄로 명시하는 방식
- 예시 2개(충분/부족) 쌍을 전송부 안에 두는 방식
- 기준 정보를 `{자리}`로 두고 코드가 채우는 방식

### ✍️ 우리 팀

- 시스템 지시(전송부 전체):

```
____
```

- null과 생략의 정책(스키마 B절과 같은가): ____
- 모델이 알 수 없는 기준 정보(오늘 날짜, 허용 목록 등)와 그 자리 이름: ____
- 예시 1 (정보 충분): 입력 "____" → 출력 `____`
- 예시 2 (**정보 부족**): 입력 "____" → 출력 `____`

---

## D. 실패 모드 점검표 — `parse_query.md`의 마커 밖

### 📘 단골 예시

| # | 실패 모드 | 점검 입력 | 어디서 잡히나 | 그다음 |
|---|---|---|---|---|
| 1 | (정상) | "조용한 강남 비건 식당, 둘이 저녁 7시" | 스키마 통과, 필수 조건 충족 | 검색과 랭킹으로 진행 |
| 2 | 정보 부족 | "괜찮은 데 좀 알려줘" | 스키마는 통과(null 허용). 애플리케이션의 필수 조건 검사(`area`, `party_size`가 null)에서 걸림 | 모델을 다시 부르지 않고 사용자에게 지역과 인원을 되묻는다 |
| 3 | 모호한 시간 | "강남에서 둘이 7시" | `time` 생략 → 예약 단계 전 필수 조건 검사 | 오전인지 오후인지 되묻는다 |
| 4 | 범위 위반 | 출력에 `"party_size": 0` | 스키마 `minimum: 1` 위반 → 검증 실패 | 오류 메시지를 붙여 1회 재요청 |
| 5 | 지원 밖 조건 | "저탄고지 되는 데 강남에서 둘이" | 프롬프트 규칙으로 `dietary`에서 제외되어 스키마 통과. 값이 새면 `enum` 위반 → 검증 실패 | 확정된 조건("강남, 2명, 식이제약 없음")을 사용자에게 보여 주고 확인받는다 — 조용히 버리면 사용자는 조건이 반영됐다고 믿는다 |
| 6 | 환각 `area` | "역 근처 아무 데나 둘이" | 후처리에서 허용 지역 목록과 대조 → 불일치 | `area`를 null로 바꾸고 되묻는다 |
| 7 | 스키마에 없는 키 | 출력에 `"mood": …` | `additionalProperties: false` → 검증 실패 | 오류 메시지를 붙여 1회 재요청 |
| 8 | JSON 아님 또는 절단 | `stop_reason`이 `max_tokens`, 파싱 실패 | 응답 상태 확인과 JSON 파싱 | 1회 재요청 |

- **재시도 정책**: 재요청은 형식 오류(4, 7, 8)에만 한다. 정보 부족(2, 3)은 같은 입력을 다시 보내도 없는 정보가 생기지 않으므로 재요청하지 않고 되묻는다. 2회 연속 형식 오류면 파싱을 포기하고 사용자에게 되묻는다. 추측해서 진행하지 않는다.

### 💬 왜 이렇게 썼나

- **실패 모드를 "무엇이 잘못될 수 있나"가 아니라 "어디서 잡히고 그다음 무엇을 하나"로 적었다.** 오른쪽 두 열이 비면 그건 잡히지 않는 실패다.
- **점검 입력을 행마다 적었다.** 표만 있고 입력이 없으면 점검이 아니라 희망이다. 프롬프트나 스키마를 바꿀 때마다 이 입력들을 다시 돌린다 — 강의 7의 회귀 세트가 여기서 시작한다.
- **방어가 세 층이다.** 스키마 검증(형식), 후처리 대조(의미), 필수 조건 검사(실행 가능 여부). `area: "판교역 근처 어딘가"`는 스키마를 통과하지만 검색이 안 되고, `{"area":null}`은 스키마를 통과하지만 실행할 수 없다. 형식 검증만으로는 부족하다.
- **재시도 대상을 형식 오류로 한정했다.** 정보가 없는 상태에서 모델만 다시 부르면 비용만 들고 없는 정보가 생기지 않는다. 상한이 없으면 무한 루프, 폴백이 없으면 결국 추측으로 진행한다. "모르면 되묻는다"가 이 제품의 기본값이다.
- **5번은 v1의 한계를 드러낸 행이다.** 제외된 조건을 파서 출력만 보고는 알 수 없다. 확정 조건을 사용자에게 되비추는 것으로 대신하고, 되물을 항목을 담는 필드는 온톨로지를 고치는 v2 결정으로 적어 두었다. 한계를 숨기지 않고 어디서 막는지 적는 것이 점검표의 역할이다.

🔁 **반드시 달라지는 곳** — 실패 모드 목록과 점검 입력(우리 필드에서 실제로 나는 오류를 몇 번 돌려 보고 적는다).

🧩 **따라 해도 되는 곳**
- 표의 다섯 열(번호 / 실패 모드 / 점검 입력 / 어디서 잡히나 / 그다음)
- 형식, 의미, 실행 가능 여부의 3층 방어
- 재시도를 형식 오류로 한정하고 상한과 폴백을 명시하는 방식
- 정상, 정보 부족, 모호한 값, 범위 위반, 지원 밖 조건의 다섯 가지 점검 입력을 기본으로 두는 방식

### ✍️ 우리 팀

| # | 실패 모드 | 점검 입력 | 어디서 잡히나 | 그다음 |
|---|---|---|---|---|
| 1 | (정상) | "____" | ____ | ____ |
| 2 | 정보 부족 | "____" | ____ | ____ |
| 3 | 모호한 값 | "____" | ____ | ____ |
| 4 | 범위 위반 | ____ | ____ | ____ |
| 5 | 지원 밖 조건 | "____" | ____ | ____ |
| ____ | ____ | ____ | ____ | ____ |

- 재요청하는 오류 유형: ____ / 재요청 횟수: ____ / 초과 시 폴백 동작: ____

---

## 제출 전 자가 점검

- [ ] 스키마의 프로퍼티가 **강의 2 온톨로지의 해당 클래스 속성과 완전히 일치**하는가
- [ ] 모든 필드가 "어느 함수의 어느 인자로 가는가"에 답할 수 있고, 그 표가 `parse_query.md`에 있는가
- [ ] `required`가 최소 집합이고, required 키의 타입이 `"null"`을 허용하는가
- [ ] `additionalProperties: false`가 있는가
- [ ] 닫힌 집합에만 enum을 걸었는가
- [ ] `parse_query.md`에 `<!-- prompt:start -->`와 `<!-- prompt:end -->` 마커가 있고, 코드가 그 사이만 읽는가
- [ ] `prompt_version`과 변경 이력이 있는가
- [ ] 프롬프트 예시에 **정보가 모자란 발화**가 최소 1개 있는가
- [ ] 실패 모드 표의 점검 입력 열과 "그다음" 열이 전부 채워졌는가
- [ ] 실제로 다섯 가지 점검 입력을 돌려 보고 결과를 기록했는가

## 자가 점검 루브릭

- 스키마가 후속 단계(검색, 랭킹, 예약)에 충분한가 … 35%
- 환각과 누락 방어 설계(enum, required와 null, 후처리, 되묻기) … 35%
- 예시와 실패 모드 점검표의 구체성 … 30%
